name: embedding-recommender
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pandas>=2.1
  - pip
  - pip:
      - anthropic>=0.40
      - jsonschema>=4.21
      - python-dotenv>=1.0
      - pytest>=7.4
